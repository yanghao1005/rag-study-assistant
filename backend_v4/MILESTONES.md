# backend_v4 Milestones

## M1 - Objective and Scope Locked

Definition of done:

- Objective, architecture, API contracts, and delivery plan reviewed.
- MVP and non-goals explicitly documented.

Output:

- approved documentation baseline.

## M2 - Foundation Running

Definition of done:

- FastAPI skeleton running with health endpoint.
- config validation, logging, and error contract in place.

Output:

- stable developer baseline.

## M3 - Ingestion Operational

Definition of done:

- PDF/summary ingestion pipeline works asynchronously.
- stage-level debugging and run status endpoint available.

Output:

- indexed documents with metadata-rich chunks.

## M4 - Retrieval Quality Gate

Definition of done:

- hybrid retrieval + rerank integrated.
- minimum relevance threshold achieved on benchmark set.

Output:

- reliable context retrieval for generation.

## M5 - Generation Quality Gate

Definition of done:

- flashcard/quiz/summary endpoints stable.
- schema compliance and grounding checks pass.

Output:

- frontend-ready generation APIs.

## M6 - Optional Advanced Mode

Definition of done:

- graph retrieval and optional agentic mode behind flags.
- fallback path to deterministic mode verified.

Output:

- advanced capability without sacrificing reliability.

## M7 - Thesis Validation Package

Definition of done:

- benchmark report generated (quality, latency, cost).
- architecture and results documented for thesis chapter.

Output:

- reproducible technical evidence for final write-up.
